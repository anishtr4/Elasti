# Environment Variables

# Main Provider (sets defaults for both Chat and Embeddings)
# Options: gemini, openai (supports both), groq (chat only)
LLM_PROVIDER=gemini

# Optional Overrides (uncomment to swap specific providers)
# CHAT_PROVIDER=groq
# EMBEDDING_PROVIDER=openai

# API Keys (only the one matching LLM_PROVIDER is required)
GEMINI_API_KEY=your_gemini_api_key_here
GROQ_API_KEY=gsk_your_groq_key_here
OPENAI_API_KEY=your_openai_api_key_here

# Infrastructure
OPENSEARCH_URL=http://localhost:9200
REDIS_URL=redis://localhost:6380
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/elasti
